import { Environment } from '../environment.types';

/**
 * Just take this configuration as example
 */
export const environment: Environment = {
  production: false,
  webSocketEndpoint: 'api/websocket',
  restEndpoint: 'api',
};
